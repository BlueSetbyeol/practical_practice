import "./TestOfCors.css";
// import { useEffect, useState } from "react";

export default function TestOfCors() {
  // const randomNum = Math.floor(1 + Math.random() * 731);
  // const [getHero, setGetHero] = useState();

  // useEffect(() => {
  //   fetch(
  //     `https://superheroapi.com/api/${import.meta.env.VITE_API_KEY}/${randomNum}`,
  //   )
  //     .then((resp) => resp.json())
  //     .then((data) => setGetHero(data));
  // }, [randomNum]);

  return (
    <>
      <h1>Some difficult API about Heroes</h1>
    </>
  );
}
